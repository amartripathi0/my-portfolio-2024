/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.sanity.io", "via.placeholder.com" , 'roboto.studio/logo.svg', 'media.licdn.com'],
    },
}

export default nextConfig
